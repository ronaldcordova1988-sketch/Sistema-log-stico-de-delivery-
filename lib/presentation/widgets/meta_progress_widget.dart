import 'package:flutter/material.dart';

class MetaProgressWidget extends StatelessWidget {
  const MetaProgressWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      // TODO: Implementar MetaProgressWidget
      child: const Text('MetaProgressWidget'),
    );
  }
}
