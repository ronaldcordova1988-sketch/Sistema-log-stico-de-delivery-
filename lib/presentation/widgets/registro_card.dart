import 'package:flutter/material.dart';

class RegistroCard extends StatelessWidget {
  const RegistroCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      // TODO: Implementar RegistroCard
      child: const Text('RegistroCard'),
    );
  }
}
