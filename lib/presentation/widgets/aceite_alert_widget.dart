import 'package:flutter/material.dart';

class AceiteAlertWidget extends StatelessWidget {
  const AceiteAlertWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      // TODO: Implementar AceiteAlertWidget
      child: const Text('AceiteAlertWidget'),
    );
  }
}
